/**
 * Dados de entrada para o caso de uso ObterTabelasAtivas.
 *
 * @module Dicionario
 */
export interface ObterTabelasAtivasInput {
  /**
   * Token JWT do usuário autenticado para acesso à Remote API Sankhya.
   */
  tokenUsuario: string;
}
