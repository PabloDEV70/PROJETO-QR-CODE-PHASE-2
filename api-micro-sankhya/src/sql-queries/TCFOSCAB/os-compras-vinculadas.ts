/** Notas fiscais/requisicoes vinculadas a uma OS via NUOSCAB */
export const osComprasVinculadas = `SELECT C.NUNOTA, C.NUMNOTA, C.CODTIPOPER, T.DESCROPER AS TIPO_OPER_DESCRICAO, C.TIPMOV, C.STATUSNOTA, C.VLRNOTA, C.NUMCOTACAO, CONVERT(VARCHAR(10),C.DTNEG,120) AS DTNEG, U.NOMEUSU AS NOME_USUARIO FROM TGFCAB C LEFT JOIN TGFTOP T ON T.CODTIPOPER=C.CODTIPOPER AND T.DHALTER=(SELECT MAX(T2.DHALTER) FROM TGFTOP T2 WHERE T2.CODTIPOPER=C.CODTIPOPER AND T2.DHALTER<=C.DTNEG) LEFT JOIN TSIUSU U ON U.CODUSU=C.CODUSUINC WHERE C.NUOSCAB=@nuos ORDER BY C.NUNOTA`;

/** Itens de todas as requisicoes vinculadas a uma OS */
export const osComprasItens = `SELECT I.NUNOTA, I.SEQUENCIA, I.CODPROD, P.DESCRPROD AS PRODUTO_DESCRICAO, I.QTDNEG, I.QTDENTREGUE, I.QTDNEG-ISNULL(I.QTDENTREGUE,0) AS QTD_PENDENTE, I.PENDENTE, RTRIM(ISNULL(I.AD_SITUACAOPRODCOT,'')) AS STATUS_COTACAO, I.NUMPEDIDO, I.VLRUNIT, I.VLRTOT, P.CODVOL AS UNIDADE FROM TGFITE I LEFT JOIN TGFPRO P ON P.CODPROD=I.CODPROD WHERE I.NUNOTA IN (SELECT C.NUNOTA FROM TGFCAB C WHERE C.NUOSCAB=@nuos) ORDER BY I.NUNOTA, I.SEQUENCIA`;
